# Database Settings (if not PostgreSQL, change pom.xml to use JDBC for your DBMS)

database.driver = org.postgresql.Driver
database.address = jdbc:postgresql://localhost:5432/database
database.user = user
database.password = password
database.poolSize = 4

# Restless Settings